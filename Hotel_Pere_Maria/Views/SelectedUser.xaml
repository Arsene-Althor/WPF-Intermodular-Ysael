<Window x:Class="Hotel_Pere_Maria.Views.SelectedUser"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Hotel_Pere_Maria.Views"
        mc:Ignorable="d"
        Title="SelectedUser" Height="650" Width="1000"
        WindowStartupLocation="CenterScreen" Background="#F5F6F7">
    
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"></RowDefinition>
        </Grid.RowDefinitions>
        <Border>
            <TextBlock FontSize="20" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,20" FontFamily="Segoe UI Black">Listado de Usuarios</TextBlock>
        </Border>

        <Border Grid.Row="1" Background="White" CornerRadius="12" Padding="15" Margin="0,0,0,20">
            <Border.Effect>
                <DropShadowEffect BlurRadius="15" Opacity="1" ShadowDepth="1"/>
            </Border.Effect>

            <StackPanel>
                <TextBlock Text="Filtros de Búsqueda" FontWeight="Bold" FontSize="14" Margin="0,0,0,15" Foreground="#2C3E50"/>

                <UniformGrid Columns="5">
                    <StackPanel Margin="5">
                        <TextBlock Text="ID Usuario" FontSize="11" Margin="0,0,0,3" FontWeight="SemiBold"/>
                        <TextBox x:Name="txtFiltroIdUser" Height="30" VerticalContentAlignment="Center" TextChanged="FiltrarDatos"/>
                    </StackPanel>
                    <StackPanel Margin="5">
                        <TextBlock Text="DNI" FontSize="11" Margin="0,0,0,3" FontWeight="SemiBold"/>
                        <TextBox x:Name="txtFiltroDNI" Height="30" VerticalContentAlignment="Center" TextChanged="FiltrarDatos"/>
                    </StackPanel>

                    <StackPanel Margin="5">
                        <TextBlock Text="Nombre" FontSize="11" Margin="0,0,0,3" FontWeight="SemiBold"/>
                        <TextBox x:Name="txtFiltroNombre" Height="30" VerticalContentAlignment="Center" TextChanged="FiltrarDatos"/>
                    </StackPanel>

                    <StackPanel Margin="5">
                        <TextBlock Text="Apellido" FontSize="11" Margin="0,0,0,3" FontWeight="SemiBold"/>
                        <TextBox x:Name="txtFiltroApellido" Height="30" VerticalContentAlignment="Center" TextChanged="FiltrarDatos"/>
                    </StackPanel>

                    <StackPanel Margin="5">
                        <TextBlock Text="Email" FontSize="11" Margin="0,0,0,3" FontWeight="SemiBold"/>
                        <TextBox x:Name="txtFiltroEmail" Height="30" VerticalContentAlignment="Center" TextChanged="FiltrarDatos"/>
                    </StackPanel>
                </UniformGrid>
                <Button Grid.Column="3" Content="Limpiar" Click="Click_LimpiarFiltros" Width="80" Height="35" VerticalAlignment="Bottom" Margin="5" Background="#E74C3C" Foreground="White" FontWeight="Bold">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="5"/>
                        </Style>
                    </Button.Resources>
                </Button>
            </StackPanel>
        </Border>

        <Border Grid.Row="2" Background="White" CornerRadius="12" Padding="10">
            <Border.Effect>
                <DropShadowEffect BlurRadius="15" Opacity="1" ShadowDepth="1"/>
            </Border.Effect>
            <DataGrid x:Name="dgUsuarios" 
                  AutoGenerateColumns="False" 
                  IsReadOnly="True"
                  GridLinesVisibility="Horizontal"
                  HorizontalGridLinesBrush="#F0F0F0"
                  RowHeight="40"
                  Background="White"
                  BorderThickness="0"
                  MouseDoubleClick="dbClick_SelectUser">

                <DataGrid.ColumnHeaderStyle>
                    <Style TargetType="DataGridColumnHeader">
                        <Setter Property="Background" Value="Transparent"/>
                        <Setter Property="Foreground" Value="#34495E"/>
                        <Setter Property="FontWeight" Value="Bold"/>
                        <Setter Property="Padding" Value="10,5"/>
                        <Setter Property="Height" Value="40"/>
                    </Style>
                </DataGrid.ColumnHeaderStyle>

                <DataGrid.Columns>
                    <DataGridTextColumn Header="ID Usuario" Binding="{Binding user_id}" Width="*" FontWeight="SemiBold"/>
                    <DataGridTextColumn Header="DNI" Binding="{Binding dni}" Width="*"/>
                    <DataGridTextColumn Header="Nombre" Binding="{Binding name}" Width="*"/>
                    <DataGridTextColumn Header="Apellido" Binding="{Binding surname}" Width="*"/>
                    <DataGridTextColumn Header="Email" Binding="{Binding email}" Width="*"/>
                    <DataGridTextColumn Header="Descuento" Binding="{Binding discont, StringFormat={}{0:N2}€}" Width="*"/>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
        <Border Grid.Row="4" Background="White" CornerRadius="12" Padding="15" Margin="0,20,0,0">
            <Border.Effect>
                <DropShadowEffect BlurRadius="15" Opacity="1" ShadowDepth="1"/>
            </Border.Effect>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                
                <Button Content="ACEPTAR" Margin="5" Height="40" Width="200" Background="#2ECC71" Foreground="White" FontWeight="Bold" BorderThickness="0">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="10"/>
                        </Style>
                    </Button.Resources>
                </Button>
                <Button Content="CANCELAR" Margin="5" Height="40" Width="200" Background="#E74C3C" Foreground="White" BorderThickness="0">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="10"/>
                        </Style>
                    </Button.Resources>
                </Button>
            </StackPanel>
        </Border>
    </Grid>
</Window>
